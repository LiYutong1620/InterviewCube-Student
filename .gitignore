# ============================================================
# 面立方学生端 · 根目录忽略规则
# ------------------------------------------------------------
# 只放「本地专属、不该进仓库」的内容；
# 子模块各自的规则见 RuoYi-Vue/.gitignore、RuoYi-Vue3/.gitignore。
# ============================================================

# 本地进度文档：含个人决策记录、待办与风险清单，仅本地维护
# /PLAN.md

# AI 助手工作目录：记忆文件（memory/）+ 临时脚本（tmp/）
/.workbuddy-ai/
